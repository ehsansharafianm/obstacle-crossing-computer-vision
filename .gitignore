# --- Video files (large; keep out of git) ---------------------------------
*.MOV
*.mov
*.MP4
*.mp4
*.avi
*.AVI

# --- Python ---------------------------------------------------------------
.venv/
__pycache__/
*.pyc

# --- Raw inputs & regenerable intermediates (large; not versioned) ---------
# raw clips + extracted calibration frame dumps
code/data/
# old flat scratch results (superseded by sessions/)
code/results/

# --- Keep per-session RESULTS (small) but never the raw videos -------------
# CSV / PNG / run.txt under sessions/ and calibration/ are intentionally kept.
